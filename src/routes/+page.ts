// import type { PageLoad } from "./$types";
// import { OPENWEATHER_API_KEY, DOMAIN_API } from "$env/static/private";

// export const load = (({ data }) => {
//   return { data };
// }) satisfies PageLoad;
